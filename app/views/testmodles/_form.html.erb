<%= form_for(@testmodle) do |f| %>
  <% if @testmodle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testmodle.errors.count, "error") %> prohibited this testmodle from being saved:</h2>

      <ul>
      <% @testmodle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
